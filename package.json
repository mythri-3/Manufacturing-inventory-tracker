{
  "name": "manufacturing-inventory-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack inventory tracker for bottle production materials.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
